<!DOCTYPE html>
<html lang="hi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login - AI Chatbot</title>
    <link rel="stylesheet" href="/styles/auth.form.css">
</head>
<body>
    <div class="register-container">
        <div class="register-card">
            <div class="register-header">
                <h1 class="register-title">Welcome Back</h1>
                <p class="register-subtitle">Sign in to your AI Chatbot account</p>
            </div>

            <form class="register-form" id="loginForm" method="post" action="/auth/login">
                <div class="form-group">
                    <label for="email" class="form-label">Email Address</label>
                    <input 
                        type="email" 
                        id="email" 
                        name="email" 
                        class="form-input" 
                        placeholder="Enter your email"
                        required
                    >
                </div>

                <div class="form-group">
                    <label for="password" class="form-label">Password</label>
                    <input 
                        type="password" 
                        id="password" 
                        name="password" 
                        class="form-input" 
                        placeholder="Enter your password"
                        required
                        minlength="8"
                    >
                </div>

                <button type="submit" class="submit-button" id="submitBtn">
                    Sign In
                </button>
            </form>

            <div class="login-link">
                Don't have an account? <a href="/register">Create account here</a>
            </div>
        </div>
    </div>
</body>
</html>
